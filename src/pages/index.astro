---
// src/pages/index.astro — Home page (English – default locale, no /en/ prefix)
import BaseLayout from '@/layouts/BaseLayout.astro';
import { useTranslations } from '@/lib/i18n';
import HeroIsland from '@/components/islands/HeroIsland.tsx';
import StructuredData from '@/components/StructuredData.astro';
import { PROFILE_SOCIALS } from '@/lib/links';
import founder from '@/assets/images/founder.jpeg';

const lang = 'en' as const;
const t = useTranslations(lang);

const heroProps = {
  name: t('profile.name'),
  role: t('profile.role'),
  bio: t('profile.bio'),
  photoSrc: founder.src,
  photoAlt: t('profile.photoLabel'),
  socials: PROFILE_SOCIALS,
};
---

<BaseLayout
  title={t('profile.metaTitle')}
  description={t('profile.metaDescription')}
  lang={lang}
  alternateUrl="https://jesus.vanguarddevs.com/es/"
>
  <StructuredData schemas={[{ type: 'person', lang: 'en' }]} />
  <HeroIsland {...heroProps} client:load />
</BaseLayout>
